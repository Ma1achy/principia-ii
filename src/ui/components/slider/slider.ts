/**
 * @fileoverview Slider Component
 * Custom range sliders with visual markers, fill indicators, and value displays
 * 
 * Styling contract:
 * - slider.css defines all visual appearance
 * - This JS handles DOM generation and behavior
 * - CSS must be loaded via <link> tag in index.html
 * 
 * NOTE: This file contains DEPRECATED exports that have been moved to builders/sliders.ts
 * It exists only for backward compatibility during migration.
 */

// Re-export everything from the builders module for backward compatibility
export * from '../../builders/sliders.js';
